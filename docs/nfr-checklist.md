# 📋 NFR Checklist (Performance-first)

## Час
* Усі дати у форматі **`ISO 8601`**, часовий пояс **`UTC`**.

## Коди помилок
* Стабільні коди: **`BOOKING_FULL`**, **`INVALID_TIMESLOT`**, **`NOT_FOUND`**, **`UNAUTHORIZED`**.
    * *Реалізація:* В `app.js` імітується помилка `BOOKING_FULL` після `POST /bookings`.

##  Performance
* Пагінація всіх списків: `limit + offset`.
    * *Реалізація:* В `app.js` імітується використання пагінації для **GET /timeslots** (вибираємо лише перший елемент масиву).
* Відповіді API не більше 2-5 КБ.
    * *Реалізація:* Мок-файли (`timeslots.json`, `services.json`) є **мінімальними** за розміром, що демонструє дотримання вимоги.
* Кеш популярних таймслотів (10 хв).
    * *Реалізація:* В `app.js` **імітується швидке завантаження** таймслотів (менша затримка `700ms` порівняно з `500ms` для послуг), що симулює кешування.
* Стиснення відповідей (`gzip/br`).
    * *Реалізація:* У прототипі не реалізовується, але прописано як архітектурне рішення (відмічено в `README.md`).

##  Security
* HTTPS для всіх запитів.
* Токен-авторизація для `admin/master`.

##  Availability
* 99% uptime для API.

---

##  A11y (Accessibility) Акценти
* **Контраст:** Використано темну тему в `styles.css` з високим контрастом (світлий текст на темному фоні, акцентні кольори).
* **Клавіша фокусу (`Tabindex`):** Всі інтерактивні елементи (`button`, `input`) мають чіткі стилі `:focus` та логічний `tabindex` в `index.html`.
* **ARIA-атрибути:**
    * Використовуються `aria-label` для кнопок.
    * Використовується `aria-live="polite"`/`"assertive"` для повідомлень про стан (завантаження, успіх/помилка), щоб повідомити користувачів, які користуються скрінрідерами.
